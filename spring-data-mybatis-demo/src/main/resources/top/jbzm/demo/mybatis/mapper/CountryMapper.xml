<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.jbzm.demo.mybatis.mapper.CountryMapper">
    <select id="selectByCountryName" resultType="top.jbzm.demo.mybatis.entity.Country">
        select * from country where countryname = #{countryname}

    </select>
</mapper>